# Ambiente Local
DB_HOST=localhost
DB_PORT=5432
DB_NAME=crypto_dashboard
DB_USER=local_user
DB_PASSWORD=local_password

# Ambiente Azure (NÃO COLOCAR SEGREDOS REAIS AQUI)
# Copiar estas chaves para App Settings no Portal (Configuration)
# Formato típico do utilizador flexível PostgreSQL: app_user@servername
AZURE_DB_HOST=yourserver.postgres.database.azure.com
AZURE_DB_PORT=5432
AZURE_DB_NAME=crypto_dashboard
AZURE_DB_USER=app_user@yourserver
AZURE_DB_PASSWORD=REPLACE_IN_PORTAL

# NOTA:
# - Este ficheiro serve apenas de exemplo. Não commitar .env com passwords reais.
# - Em produção usar exclusivamente Application Settings do Azure App Service.# Exemplo de variáveis de ambiente para conectar ao PostgreSQL
# Copia este ficheiro para `.env` e preenche os valores reais.

DB_NAME=cryptodashboard
DB_USER=postgres
DB_PASSWORD=changeme
DB_HOST=localhost
DB_PORT=5432

# Opcional: configurações de ambiente para desenvolvimento
FLASK_ENV=development
STREAMLIT_SERVER_RUN_ON_SAVE=true
